import { Provider, Type } from '@angular/core';
import { BaseSpectatorOptions } from '@ngneat/spectator/lib/base/options';
/**
 * @publicApi
 */
export interface SpectatorDirectiveOptions<D, H> extends BaseSpectatorOptions {
    directive: Type<D>;
    shallow?: boolean;
    detectChanges?: boolean;
    host?: Type<H>;
    template?: string;
    directiveProviders?: Provider[];
    directiveMocks?: Type<any>[];
    declareDirective?: boolean;
}
/**
 * @internal
 */
export declare function getSpectatorDirectiveDefaultOptions<D, H>(overrides?: SpectatorDirectiveOptions<D, H>): Required<SpectatorDirectiveOptions<D, H>>;
