import { Router, Event } from '@angular/router';
export declare abstract class RouterStub extends Router {
    abstract emitRouterEvent(event: Event): void;
    abstract serializeUrl(): string;
}
export declare function isRouterStub(router: Router): router is RouterStub;
