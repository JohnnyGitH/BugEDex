import { NgModule, Provider, SchemaMetadata, Type } from '@angular/core';
import { MetadataOverride, ModuleTeardownOptions } from '@angular/core/testing';
import { MockProvider } from '@ngneat/spectator/lib/mock';
/**
 * @internal
 */
export interface BaseSpectatorOptions {
    disableAnimations?: boolean;
    entryComponents?: Type<any>[];
    mocks?: Type<any>[];
    mockProvider?: MockProvider;
    providers?: any[];
    declarations?: any[];
    imports?: any[];
    schemas?: (SchemaMetadata | any[])[];
    overrideModules?: [Type<any>, MetadataOverride<NgModule>][];
    teardown?: ModuleTeardownOptions;
}
/**
 * @internal
 */
export interface BaseSpectatorOverrides {
    providers?: Provider[];
}
/**
 * @internal
 */
export declare function getDefaultBaseOptions(options?: BaseSpectatorOptions): Required<BaseSpectatorOptions>;
