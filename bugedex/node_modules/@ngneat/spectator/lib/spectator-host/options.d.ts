import { Type } from '@angular/core';
import { SpectatorOptions } from '@ngneat/spectator/lib/spectator/options';
/**
 * @publicApi
 */
export interface SpectatorHostOptions<C, H> extends SpectatorOptions<C> {
    host?: Type<H>;
    template?: string;
}
/**
 * @internal
 */
export declare function getSpectatorHostDefaultOptions<C, H>(overrides?: SpectatorHostOptions<C, H>): Required<SpectatorHostOptions<C, H>>;
