import { DebugElement } from '@angular/core';
import { ComponentFixture } from '@angular/core/testing';
import { SpyObject } from '@ngneat/spectator/lib/mock';
import { Token } from '@ngneat/spectator/lib/token';
import { DomSpectator } from '@ngneat/spectator/lib/base/dom-spectator';
/**
 * @publicApi
 */
export declare class Spectator<C> extends DomSpectator<C> {
    fixture: ComponentFixture<C>;
    debugElement: DebugElement;
    protected instance: C;
    element: HTMLElement;
    constructor(fixture: ComponentFixture<C>, debugElement: DebugElement, instance: C, element: HTMLElement);
    get component(): C;
    inject<T>(token: Token<T>, fromComponentInjector?: boolean): SpyObject<T>;
    detectComponentChanges(): void;
}
