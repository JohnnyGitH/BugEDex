/// <amd-module name="@angular/compiler-cli/ngcc/src/execution/cluster/executor" />
/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */
import { PathManipulation } from '@angular/compiler-cli/src/ngtsc/file_system';
import { Logger } from '@angular/compiler-cli/src/ngtsc/logging';
import { AsyncLocker } from '@angular/compiler-cli/ngcc/src/locking/async_locker';
import { FileWriter } from '@angular/compiler-cli/ngcc/src/writing/file_writer';
import { PackageJsonUpdater } from '@angular/compiler-cli/ngcc/src/writing/package_json_updater';
import { AnalyzeEntryPointsFn, CreateCompileFn, Executor } from '@angular/compiler-cli/ngcc/src/execution/api';
import { CreateTaskCompletedCallback } from '@angular/compiler-cli/ngcc/src/execution/tasks/api';
/**
 * An `Executor` that processes tasks in parallel (on multiple processes) and completes
 * asynchronously.
 */
export declare class ClusterExecutor implements Executor {
    private workerCount;
    private fileSystem;
    private logger;
    private fileWriter;
    private pkgJsonUpdater;
    private lockFile;
    private createTaskCompletedCallback;
    constructor(workerCount: number, fileSystem: PathManipulation, logger: Logger, fileWriter: FileWriter, pkgJsonUpdater: PackageJsonUpdater, lockFile: AsyncLocker, createTaskCompletedCallback: CreateTaskCompletedCallback);
    execute(analyzeEntryPoints: AnalyzeEntryPointsFn, _createCompileFn: CreateCompileFn): Promise<void>;
}
