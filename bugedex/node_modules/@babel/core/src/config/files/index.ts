type indexBrowserType = typeof import("@babel/core/src/config/files/index-browser");
type indexType = typeof import("@babel/core/src/config/files");

// Kind of gross, but essentially asserting that the exports of this module are the same as the
// exports of index-browser, since this file may be replaced at bundle time with index-browser.
({} as any as indexBrowserType as indexType);

export { findPackageData } from "@babel/core/src/config/files/package";

export {
  findConfigUpwards,
  findRelativeConfig,
  findRootConfig,
  loadConfig,
  resolveShowConfigPath,
  ROOT_CONFIG_FILENAMES,
} from "@babel/core/src/config/files/configuration";
export type {
  ConfigFile,
  IgnoreFile,
  RelativeConfig,
  FilePackageData,
} from "@babel/core/src/config/files/types";
export {
  resolvePlugin,
  resolvePreset,
  loadPlugin,
  loadPreset,
} from "@babel/core/src/config/files/plugins";
